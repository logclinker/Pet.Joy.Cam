# Hub bind
HOST=127.0.0.1
PORT=1212

# Where to store keys + frames
DATA_DIR=./data

# Optional: enable flash control password prompt (recommended)
# FLASH_PASS=change-me

# Optional: alternate admin token auth (bypasses FLASH_PASS)
# ADMIN_TOKEN=change-me
